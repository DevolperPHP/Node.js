// NODE JS

a = [1, 2, 3, 4, 5, 10]; // 10 is bigger  than 5

for (i = 0; i < 5; i++) {
    console.log(a[i]); // when you run the file it won't print number 10 because it bigger than 5
}

setTimeout(function () {
    process.exit();
}, 500000);
