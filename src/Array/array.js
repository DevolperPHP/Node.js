// First we should make the array by using var *array_name* = [items]

var name = ['Ali' , 'Mohammed' , 'Abd'];

// Now we crated the array let's print it

// to print it we use console.log(*array_name*);

console.log(name);
